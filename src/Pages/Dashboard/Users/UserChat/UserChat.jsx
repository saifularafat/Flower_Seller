import { Helmet } from "react-helmet-async";

const UserChat = () => {
    return (
        <div>
            <Helmet>
                <title>Flower Shop || Chat </title>
            </Helmet>
            <h4>Chat</h4>
        </div>
    );
};

export default UserChat;